import './main.css';
import React from 'react';

class Main extends React.Component {
  constructor({}, {}) {
    super({}, {});

    this.state = [];
  }

  render(): React.ReactNode {
    return (
      <main className="main">
        <h2>Content</h2>
      </main>
    );
  }
}

export default Main;
